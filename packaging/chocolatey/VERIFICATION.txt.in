VERIFICATION

The packaged ragavan.exe was built from the source tagged at:
https://github.com/chiefGui/ragavan/tree/v{{VERSION}}

Build command:
cargo build --locked --release --package ragavan

The repository pins the Rust toolchain used for the build in
rust-toolchain.toml. To verify the packaged executable, build that tag with the
command above and compare the SHA-256 checksum.

Packaged ragavan.exe SHA-256:
{{SHA256}}
